"""
BharatAlpha — Stock Analysis & Recommendation helpers
=====================================================

Jupyter notebook-side helpers that compose the existing
`backend.engine` modules (technicals, fundamentals, trade_planner)
with Alpha Vantage enrichment, on-disk caching, and visualization.

Importable as `src.*` from the notebook when the project root is on
sys.path (the notebook does `sys.path.append('..')`).
"""
